import { AggregatorPipe } from './aggregator.pipe';

describe('AggregatorPipe', () => {
  it('create an instance', () => {
    const pipe = new AggregatorPipe();
    expect(pipe).toBeTruthy();
  });
});
